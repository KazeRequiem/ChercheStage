{# filepath: templates/partials/card_pilote.html.twig #}
<div class="candidature-item">
    <div>
        <h4>{{ pilote.nom }}</h4> {# Nom du pilote #}
        <p>🧑‍✈️ {{ pilote.prenom }}</p> {# Prénom du pilote #}
        <p>📧 Email : {{ pilote.email }}</p> {# Email du pilote #}
        <p>📞 Téléphone : {{ pilote.tel }}</p> {# Téléphone du pilote #}
    </div>
    <div>
        <a href="/modifier_pilote.php?id={{ pilote.id_user }}" class="modifier">Modifier</a> {# Lien pour modifier le pilote #}
    </div>
</div>