{# filepath: templates/partials/offer_card.html.twig #}
<a href="offre_emploi.php">
    <div class="candidature-item">
        <div>
            <h4>{{ offer.title }}</h4>
            <p>🏢 {{ offer.company }}</p>
            <p>📄 {{ offer.contractType }}</p>
            <p>💰 {{ offer.salary }}</p>
            <p>📍 {{ offer.location }}</p>
        </div>
        <div class="candidature-actions">
            <img src="assets/{{ offer.logo }}" alt="{{ offer.company }} Logo" class="company-logo">
        </div>
    </div>
</a>