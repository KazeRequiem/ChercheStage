{# filepath: templates/modifier_pilote.html.twig #}
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Modifier pilote</title>
        <link rel="stylesheet" href="src/ajouter-entreprise.css">
        <link rel="stylesheet" href="src/header-footer.css">
    </head>
    <body>
        {# Inclure le header réutilisable #}
        {% include 'partials/header.html.twig' %}
        <main>
            <h2>Modifier un pilote</h2>
            {# Afficher les messages d'erreur si présents #}
            {% if app.request.query.get('error') is defined and app.request.query.get('error') is not empty %}
                <div class="alert alert-danger">
                    {{ app.request.query.get('error') }}
                </div>
            {% endif %}
            
            <form class="formulaire-ajout" method="POST" action="/modifier_pilote.php?id={{ pilote.id_user }}">
                <div class="form-group">
                    <label for="prenom">Prénom:</label>
                    <input type="text" id="prenom" name="prenom" value="{{ pilote.prenom }}">
                </div>
                
                <div class="form-group">
                    <label for="nom">Nom:</label>
                    <input type="text" id="nom" name="nom" value="{{ pilote.nom }}">
                </div>
                
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" value="{{ pilote.email }}">
                </div>
                
                <div class="form-group">
                    <label for="telephone">Téléphone:</label>
                    <input type="text" id="telephone" name="telephone" value="{{ pilote.telephone }}">
                </div>
                
                <div class="form-group">
                    <label for="datenaissance">Date de naissance:</label>
                    <input type="date" id="datenaissance" name="datenaissance" value="{{ pilote.datenaissance }}">
                </div>
                
                <div class="form-group">
                    <label for="promotion">Promotion:</label>
                    <input type="text" id="promotion" name="promotion" value="{{ pilote.promotion }}">
                </div>
                
                <input type="hidden" name="pilote_id" value="{{ pilote.id_user }}">
                <button type="submit" class="bouton-ajouter">Modifier</button>
            </form>
            
            <form method="POST" action="/modifier_pilote.php?id={{ pilote.id_user }}" class="delete-form" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet pilote ?');">
                <input type="hidden" name="_method" value="DELETE"> <!-- Indique que c'est une suppression -->
                <input type="hidden" name="pilote_id" value="{{ pilote.id_user }}">
                <button type="submit" class="bouton-supprimer">Supprimer</button>
            </form>
        </main>
        {# Inclure le footer réutilisable #}
        {% include 'partials/footer.html.twig' %}
    </body>
</html>