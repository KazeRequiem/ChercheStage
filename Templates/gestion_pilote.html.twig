<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <title>Gestion Pilote</title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="src/header-footer.css">
        <link rel="stylesheet" href="src/Page-gestion.css">
    </head>

    <body>
        {# Inclure le header réutilisable #}
        {% include 'partials/header.html.twig' %}

        <main>
            <section class="candidatures">
                {# Inclure la barre de recherche et les actions dynamiques #}
                <form method="GET" action="/gestion_pilote.php">
                    <div class="search-bar">
                        <input type="text" name="search" id="search" placeholder="Rechercher un pilote" value="{{ search|default('') }}">
                        <button type="submit" class="search-icon">🔍</button>
                    </div>
                </form>

                <div class="candidatures-publiées">
                    {% for pilote in pilotes %}
                        {% include 'partials/card_pilote.html.twig' with { pilote: pilote } %}
                    {% else %}
                        <p>Aucun pilote trouvé.</p>
                    {% endfor %}
                </div>
            </section>
        </main>

        {# Inclure le footer réutilisable #}
        {% include 'partials/footer.html.twig' %}
    </body>
</html>