<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Profil Étudiant</title>
        <link rel="stylesheet" href="src/Profil-etudiant.css">
        <link rel="stylesheet" href="src/header-footer.css">
    </head>
    <body>
        {% include 'partials/header.html.twig' %}

        <main>
            <h2>Votre profil étudiant</h2>
            <section class="contenu">
                <div class="info-section">
                    <h3>Vos informations:</h3>
                    <p><strong>Nom</strong><br>{{ student.lastName }}</p>
                    <p><strong>Prénom</strong><br>{{ student.firstName }}</p>
                    <p><strong>Email</strong><br>{{ student.email }}</p>
                    <p><strong>Promotion</strong><br>{{ student.promotion }}</p>
                </div>
                <div class="help-section">
                    <h3>Besoin d’aide? écrivez-nous!</h3>

                    {# Afficher les messages de succès ou d'erreur #}
                    {% if error %}
                        <div class="alert alert-danger">{{ error }}</div>
                    {% endif %}
                    {% if success %}
                        <div class="alert alert-success">{{ success }}</div>
                    {% endif %}

                    <form method="POST" action="">
                        <label for="objet">Objet</label>
                        <input type="text" id="objet" name="objet" placeholder="Objet" required>
                        <label for="corp">Message</label>
                        <textarea id="corp" name="corp" placeholder="Votre message" required></textarea>
                        <button type="submit" class="bouton">Envoyer le ticket</button>
                    </form>
                </div>
            </section>
        </main>

        {% include 'partials/footer.html.twig' %}
    </body>
</html>