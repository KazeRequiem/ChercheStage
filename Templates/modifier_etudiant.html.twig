{# filepath: templates/modifier_etudiant.html.twig #}
<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Modifier étudiant</title>
        <link rel="stylesheet" href="src/Profil-etudiant.css">
        <link rel="stylesheet" href="src/header-footer.css">
        <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    </head>
    <body>
        {# Inclure le header réutilisable #}
        {% include 'partials/header.html.twig' %}

        <main>
            <h2>Modifier un étudiant</h2>
            <section class="contenu">
                <div class="info-section">
                    <p><strong>Nom : </strong>{{ etudiant.nom }}</p>
                    <input type="text" placeholder="Modifier le nom">
                    <p><strong>Prénom : </strong>{{ student.prenom }}</p>
                    <input type="text" placeholder="Modifier le prénom">
                    <p><strong>Email : </strong>{{ student.email }}</p>
                    <input type="email" placeholder="Modifier le mail">
                    <p><strong>Promotion : </strong>{{ student.promotion }}</p>
                    <input type="text" placeholder="Modifier les promotions">
                    <button class="bouton">Modifier l'étudiant</button>
                </div>
            </section>
        </main>

        {# Inclure le footer réutilisable #}
        {% include 'partials/footer.html.twig' %}
    </body>
</html>